import { ContentArea } from '@/features/layout/ContentArea'

// This file is kept for backward compatibility.
// The layout is now handled by App.tsx → TeamSidebar + AgentListColumn + ContentArea.

export default function Home() {
  return <ContentArea />
}

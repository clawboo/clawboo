name: Bug Report
description: Report something that is broken
labels: ['bug']
body:
  - type: textarea
    id: description
    attributes:
      label: What happened?
      description: A clear description of the bug.
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
      description: What should have happened instead?
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: Steps to reproduce
      description: Minimal steps to reproduce the issue.
      placeholder: |
        1. Run `npx clawboo@latest`
        2. Paste a provider key in onboarding
        3. Open the Atlas view
        4. See error in console
    validations:
      required: true

  - type: input
    id: version
    attributes:
      label: Clawboo version
      description: Run `clawboo --version` or check package.json
      placeholder: 'e.g. 0.3.1'

  - type: dropdown
    id: area
    attributes:
      label: Affected area
      options:
        - Onboarding / Setup
        - Native runtime
        - Connected runtimes (Claude Code / Codex / Hermes / OpenClaw)
        - Group Chat
        - Board (tasks)
        - Atlas / Ghost Graph
        - Capabilities
        - Marketplace
        - Memory
        - Governance & budgets
        - Observability
        - Scheduler
        - Approvals
        - CLI / install
        - Other
    validations:
      required: true

  - type: textarea
    id: context
    attributes:
      label: Additional context
      description: Logs, screenshots, OS/browser info, etc.
